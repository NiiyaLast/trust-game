# 《信托游戏》需求规格说明书

**文档版本**：v1.0  
**编写日期**：2026-08-19  
**关联策划**：[GAME_DESIGN.md](./GAME_DESIGN.md)  
**文档状态**：需求基线草案

---

## 1. 文档说明

### 1.1 编写目的

本文档将《信托游戏》策划文档转化为可实现、可验收的产品需求，作为后续游戏引擎、AI适配层、前端控制台和节目录制页面的共同依据。

### 1.2 产品定位

《信托游戏》是一套面向观众的 AI 信任博弈实验系统。系统让 6 个 AI 在资金、信任、信息和道德压力下进行重复博弈，并以实时状态面板、公开交易、聊天记录、信誉榜和季终数据分析呈现过程与结果。

产品同时服务两类用户：

| 用户 | 目标 | 主要页面 |
|---|---|---|
| 观众 | 看懂局势、比较角色、捕捉背叛和反转 | 直播观战页、回放页、数据分析页 |
| 制作人员 | 创建场次、配置 AI、控制流程、处理异常 | 导演控制台、配置页、日志页 |

### 1.3 范围

本版本覆盖：

- 3 季内容模型，每季 5 场，每场 6 轮
- 6 个 AI 参赛者的状态、对话和交易记录
- 托付、受托、还款、公投和淘汰流程
- 跨场信誉榜和季终道德数据
- 观众观战界面与 OBS 录制布局
- 制作人员控制台的基础操作和异常兜底

本版本暂不包含：

- 真实观众付费、打赏和账户体系
- 对外公开的多租户云部署
- 复杂的观众实时投票商业化功能
- AI 模型训练和模型参数微调

---

## 2. 产品目标与成功标准

### 2.1 产品目标

1. 让观众在 5 秒内看懂当前轮次、每个 AI 的资金、角色和风险状态。
2. 让每一笔交易可追溯，让“谁借给谁、借了多少、还了多少”成为可验证事实。
3. 让 AI 的语言博弈与数字结果同时可见，形成可剪辑、可复盘的名场面。
4. 让制作人员可以控制一场游戏的节奏，并在 AI 输出异常时不中断节目流程。
5. 为跨场、跨季的数据分析提供统一、可导出的结构化数据。

### 2.2 可验收成功标准

| 编号 | 标准 |
|---|---|
| S-01 | 观战页首屏可同时看到当前场次、轮次、压力等级、6 名 AI 的资金和信誉分 |
| S-02 | 每次交易完成后，交易结果、余额和信誉榜在 2 秒内同步到前端 |
| S-03 | 公共频道消息按时间顺序显示；私聊仅在上帝视角面板中标记为私聊，不混入公共频道 |
| S-04 | 第 2、4 轮结束时，公投、平票重投和淘汰过程有明确状态反馈 |
| S-05 | 任意一场结束后，制作人员可以查看完整交易、聊天、投票和异常日志 |
| S-06 | 桌面端 1280px 宽度下无核心信息遮挡；移动端可以纵向查看全部核心模块 |
| S-07 | AI 输出格式错误、接口超时或金额越界时，页面显示降级状态，游戏状态机继续运行 |

---

## 3. 核心业务规则

### 3.1 赛季、场次和轮次

- 每季 5 场游戏，每场独立结算。
- 每场 6 轮，每名 AI 初始资金为 100 金币。
- 每轮根据角色轮换生成托付人和受托人名单。
- 第 2、4 轮结束后进行公投并淘汰 1 人；第 6 轮结束后结算胜者。
- 信誉榜跨场继承；资金每场重置。
- 赛季排名同时参考赢场数和赛季总信誉分。

### 3.2 交易结算

设托付人出借 `X` 金币，受托人到账 `3X` 金币，最终归还 `Y` 金币：

- 托付人净收益：`Y - X`
- 受托人净收益：`3X - Y`
- 单笔交易创造的总财富：`2X`
- 出借金额范围：0–20 金币
- 还款金额范围：0–到账金额

服务端必须以整数金额结算，前端不得自行计算并覆盖服务端结果。

### 3.3 角色与交易流程

```text
轮次开始 → 公布角色 → 公共讨论 → 私聊阶段 → 出借选择
→ 配对确认 → 到账 → 还款选择 → 结果公布 → 更新信誉榜
→ 若为第2/4轮：公投 → 平票重投（如需） → 淘汰/遗言
```

### 3.4 异常规则

- JSON 格式错误：自动重试最多 2 次。
- 重试仍失败：托付默认借 0，受托默认还 0，并记录异常原因。
- 金额越界：服务端截断到合法范围，并在制作日志中记录原始值和修正值。
- API 超时或失败：该 AI 本轮进入降级动作，不阻塞其他 AI。
- 前端断线：自动重连；重连后以服务端快照恢复，不重复提交动作。
- 任何已完成交易不得由前端回滚。

---

## 4. 功能需求

### 4.1 观战首页 / 直播观战页

观战页是产品默认首页，面向观众和 OBS 录制。页面应优先呈现当前发生的事，而不是设置入口。

#### 4.1.1 顶部状态栏

显示：

- 游戏名称《信托游戏》
- 当前季、场、轮次，例如“第一季 · 第 3 场 · 第 4/6 轮”
- 当前压力维度和压力等级
- 游戏状态：准备中、讨论中、交易中、结算中、公投中、已淘汰、已结束
- 连接状态和最后同步时间

顶部状态栏在轮次变化时有轻量状态切换，不使用大面积弹窗打断观战。

#### 4.1.2 AI 参赛者状态区

使用 6 个稳定尺寸的参赛者卡片或座位位，显示：

- AI 名称、编号、模型来源和角色头像
- 当前资金及本轮资金变化
- 跨场信誉分和受托次数
- 当前角色：托付人、受托人、轮空、已淘汰
- 本轮目标或动作状态：思考中、已发言、已出借、等待还款、已完成
- 最近一次交易对象和结果
- 关系状态：合作、怀疑、被欺骗、联盟、危险

卡片状态必须有文本标签，不能只依赖颜色。资金下降、信誉下降和淘汰状态应有数字变化与图标辅助表达。

#### 4.1.3 中央事件舞台

中央区域用于呈现当前最重要的单一事件：

- 当前配对关系
- 出借金额、三倍到账金额、实际还款金额
- 托付人收益与受托人收益
- AI 的公开承诺和实际行为
- 公投候选人与票数
- 淘汰遗言或终局结算

同一时刻只突出一个主事件，避免交易卡片、聊天弹幕和数据图表同时抢占视觉焦点。

#### 4.1.4 聊天面板

支持以下视图：

- 公共频道
- 私聊关系流
- 上帝视角全部消息
- 仅显示承诺、质疑、欺骗、道歉等关键语句

每条消息显示发送者、频道、时间、消息内容和关联轮次。私聊必须使用明确的“私聊”标记，不得仅靠背景色区分。

#### 4.1.5 交易记录面板

以时间倒序展示本场交易，并支持按轮次、AI、结果筛选。每条记录至少包含：

- 轮次和时间
- 托付人、受托人
- 出借金额、到账金额、还款金额
- 双方收益
- 是否按承诺还款
- 是否为异常降级交易

交易记录支持展开查看关联聊天和服务端结算事件。

#### 4.1.6 信誉榜

显示当前仍在场及已淘汰 AI 的：

- 当前信誉分
- 累计收到金额
- 累计归还金额
- 受托次数
- 本场变化
- 跨场排名变化

信誉分无记录时显示“暂无记录”，不得显示为 0%，避免误导观众。

#### 4.1.7 公投与淘汰

公投开始后，页面切换至公投状态，但仍保留资金和信誉摘要。显示：

- 投票倒计时
- 当前候选人
- 已公开票数和未公开票数状态
- 平票提示与 30 秒重投倒计时
- 淘汰结果和遗言

淘汰动画应控制在 2 秒以内，不影响下一轮状态同步。

### 4.2 季终数据分析页

提供单场和整季两个范围：

- 资金变化曲线
- 平均还款率
- 合作率
- 欺诈率
- 受骗次数与主动欺骗次数
- 压力等级与道德指标的相关趋势
- 关键事件时间线
- 胜场数、总信誉分和最终排名

图表需要支持悬停查看精确值、按 AI 对比和导出 CSV。图表必须同时提供表格或文本摘要，确保数据不只依赖颜色和图形。

### 4.3 导演控制台

导演控制台与观众观战页分离，默认不出现在 OBS 画面中。

功能包括：

- 新建赛季、场次和背景设定
- 配置 6 个 AI 的模型、名称、角色设定和 Prompt 版本
- 开始、暂停、继续、跳过讨论阶段
- 手动推进结算或重试失败的 AI 动作
- 查看服务端完整事件流
- 标记名场面：第一次跑路、联盟破裂、黑化、反转、集体背叛
- 导出本场 JSON、聊天 Markdown 和交易 CSV
- 查看接口耗时、失败次数和降级动作

危险操作如“结束本场”“强制淘汰”“覆盖结算”必须二次确认，并记录操作者和原因。

### 4.4 回放页

回放页允许按事件时间轴重新观看已经完成的场次：

- 播放、暂停、倍速和跳转
- 只看交易、只看公投或只看关键聊天
- 同时显示当时可见信息范围，不能把当时对 AI 隐藏的信息误标为其已知信息
- 从任意事件跳转到关联交易和聊天

---

## 5. 前端视觉设计规格

### 5.1 选用风格

本项目采用样式库中的两种风格作为参考，并进行项目化改造：

- **Runway**：借用电影化、暗色舞台、事件作为视觉主角的叙事方式，适合 AI 角色、压力背景和名场面呈现。
- **ClickHouse**：借用黑色数据驾驶舱、高密度状态信息、强调色导航和性能数字的表达方式，适合资金、信誉、交易和实时事件流。

最终方向命名为：**Cinematic Trust Console（电影化信任驾驶舱）**。

这不是两个品牌的复制。项目只借用其布局节奏、信息密度、表面层级和视觉态度，使用《信托游戏》自己的信任/背叛语义。

### 5.2 视觉目标

- 像一档冷峻、紧张的 AI 博弈直播节目，而不是普通后台管理系统。
- 让“数字事实”保持高可读，让“情绪和反转”拥有舞台感。
- 暗色背景承载长时间观战，荧光色仅用于当前事件、收益变化和关键 CTA。
- 页面有明确主次：当前事件 > 参赛者状态 > 聊天与交易 > 分析数据。

### 5.3 色彩令牌

| 令牌 | 色值 | 用途 |
|---|---|---|
| `--bg-void` | `#090a0b` | 页面主背景 |
| `--bg-stage` | `#111315` | 中央事件舞台 |
| `--bg-panel` | `#171a1d` | 状态面板、聊天面板 |
| `--bg-raised` | `#22272b` | 悬浮、展开和高层级表面 |
| `--text-primary` | `#f5f7f8` | 标题、金额、核心结果 |
| `--text-secondary` | `#a8b0b7` | 说明、消息、普通数据 |
| `--text-muted` | `#687179` | 时间、辅助标签、无记录状态 |
| `--line` | `rgba(255,255,255,.12)` | 面板和表格边界 |
| `--signal-volt` | `#d9ff45` | 当前事件、选中、关键收益 |
| `--signal-green` | `#42d392` | 合作、正收益、健康连接 |
| `--signal-red` | `#ff5c69` | 背叛、负收益、淘汰、错误 |
| `--signal-amber` | `#ffc857` | 警告、压力升级、待确认 |
| `--signal-blue` | `#70a7ff` | 信息、私聊、系统状态 |

颜色只表达语义，不以颜色作为唯一信息。红色不能大面积铺满页面；荧光黄只用于聚焦和导航，不作为装饰背景。

### 5.4 字体与数字

- 主字体：`Inter`、系统 sans-serif 回退。
- 数据和事件标识：`JetBrains Mono` 或 `ui-monospace`。
- 页面标题：32–56px，字重 600–700，紧凑行高。
- 模块标题：16–20px，字重 600。
- 正文和消息：14–16px，行高 1.45–1.6。
- 标签和时间：11–12px，可使用大写英文或中文短标签。
- 金额、轮次、票数等数字使用等宽数字，避免实时更新造成布局跳动。
- 不使用负字间距；显示文字在窄容器内必须自然换行。

### 5.5 布局规格

桌面端观战页采用三段式驾驶舱：

```text
┌──────────────────────────────────────────────────────────┐
│ 顶部状态栏：季 / 场 / 轮次 / 压力 / 连接状态              │
├──────────────────────┬──────────────────┬────────────────┤
│ AI 参赛者状态         │ 中央事件舞台      │ 聊天 / 交易流    │
│ 2×3 或 1×6 卡片       │ 当前主事件        │ 可切换标签页     │
│ 资金 / 信誉 / 角色     │ 交易 / 公投 / 结算 │ 公共 / 私聊      │
├──────────────────────┴──────────────────┴────────────────┤
│ 信誉榜 / 轮次时间线 / 关键指标 / 当前事件辅助信息          │
└──────────────────────────────────────────────────────────┘
```

- 页面最大内容宽度：1600px。
- 8px 基础间距系统，常用间距为 8、12、16、24、32px。
- 面板圆角 6px，卡片圆角 4px，禁止大面积胶囊化布局。
- 页面区块使用面板和细边界分层，不把整个页面堆叠成互相嵌套的卡片。
- 桌面端核心区至少保留 16px 内边距；OBS 画面留出安全边距。
- 事件舞台可以使用轻微的背景明暗变化，但不使用大面积渐变或装饰性光球。

### 5.6 组件规则

#### 参赛者卡片

- 默认使用 `--bg-panel` 和 `--line`。
- 当前行动者使用 `--signal-volt` 的细边框或顶部标记。
- 合作关系使用绿色小状态点，背叛或危险关系使用红色小状态点，并同时显示文字。
- 资金变化使用 `+12` / `-8` 的短时数字动画，动画结束后保留最终值。
- 已淘汰卡片降低对比度但不删除，保留历史身份。

#### 交易事件

- 出借金额、到账金额、还款金额采用三列或流程节点展示。
- 结果状态必须显示“全额归还 / 部分归还 / 跑路 / 异常降级”等文本。
- 受托人和托付人收益分别显示，避免只展示受托人最终余额。
- 关键背叛事件使用红色边框短暂强调，持续时间不超过 3 秒。

#### 按钮与控制

- 使用 Lucide 图标表达播放、暂停、筛选、展开、下载和设置等工具动作。
- 仅图标按钮必须有 tooltip 和可访问名称。
- 主要操作使用高对比实心按钮；次要操作使用透明或细边框按钮。
- “结束游戏”“强制淘汰”等危险操作使用红色语义并需要确认。
- 避免把“查看”“展开”“筛选”等熟悉动作做成过长的圆角文字胶囊。

#### 图表

- 深色底、细网格、荧光色单线突出当前 AI 或当前指标。
- 同一图表最多使用 6 条主线；超出时采用筛选而不是继续堆叠。
- 提供图例、单位、时间范围和文本数据摘要。
- 不用红绿作为唯一的正负区分，可配合箭头和“合作/背叛”标签。

### 5.7 动效

- 仅为状态变化、事件进入、金额变化和面板切换添加动效。
- 动画时长以 160–280ms 为主；淘汰或终局事件最多 600ms。
- 支持 `prefers-reduced-motion`，关闭数字跳动和过渡动画后仍保持信息完整。
- 不使用循环装饰动画，不使用会干扰阅读的闪烁。

### 5.8 响应式规则

| 视口 | 布局 |
|---|---|
| ≥1280px | 三栏驾驶舱，AI 卡片 2×3，聊天和交易流固定可见 |
| 1024–1279px | 三栏压缩，右侧面板可折叠，AI 卡片仍保持稳定尺寸 |
| 768–1023px | 两栏布局，中央事件与 AI 状态优先，聊天/交易用标签页 |
| <768px | 单栏纵向布局，顶部状态可横向滚动，模块按事件优先级排序 |
| OBS 16:9 | 提供固定的 1920×1080 安全区和紧凑版观战布局 |

移动端优先顺序：当前轮次与压力 → 中央事件 → AI 状态 → 交易记录 → 聊天 → 信誉榜 → 扩展分析。

### 5.9 可访问性

- 所有交互元素支持键盘访问，焦点状态使用清晰的高对比边框。
- 文本与背景满足 WCAG AA 对比度要求；重要金额和错误状态满足更高对比度要求。
- 颜色之外必须有图标、文字或形状辅助表达状态。
- 动态更新区域使用 `aria-live`，但避免把整屏事件流声明为 live region。
- 头像、状态图标和交易图形提供替代文本或隐藏装饰属性。

---

## 6. 信息架构与页面清单

| 页面 | 路由建议 | 访问者 | 优先级 |
|---|---|---|---|
| 实时观战页 | `/watch/:season/:match` | 观众、制作人员 | P0 |
| 导演控制台 | `/control/:season/:match` | 制作人员 | P0 |
| 场次结果页 | `/results/:season/:match` | 全部 | P1 |
| 赛季分析页 | `/analysis/:season` | 全部 | P1 |
| 回放页 | `/replay/:season/:match` | 全部 | P1 |
| AI 配置页 | `/settings/agents` | 制作人员 | P1 |
| 系统日志页 | `/system/logs` | 制作人员 | P2 |

P0 功能完成前不得进入正式录制；P1 功能可以在首个可用版本中分阶段补齐。

---

## 7. 数据模型与接口要求

### 7.1 核心实体

- `Season`：季、压力维度、当前场次、赛季排名。
- `Match`：场次背景、压力等级、初始资金、当前状态和胜者。
- `Round`：轮次、角色名单、开始/结束时间和结算状态。
- `Agent`：AI 身份、模型、角色 Prompt、资金、信誉、淘汰状态。
- `ConversationMessage`：频道、发送者、接收者、消息、时间和可见范围。
- `Transaction`：托付人、受托人、出借、到账、还款、收益和异常信息。
- `Vote`：投票者、候选人、轮次、票数和重投信息。
- `Event`：统一的可回放事件，包括状态变化、名场面和系统异常。

### 7.2 实时同步

推荐使用 WebSocket 或 Server-Sent Events 推送以下事件：

- `round.started`
- `agent.status_changed`
- `message.created`
- `transaction.proposed`
- `transaction.settled`
- `reputation.updated`
- `vote.started`
- `vote.updated`
- `agent.eliminated`
- `match.finished`
- `system.degraded`

每个事件必须包含：`event_id`、`event_type`、`server_time`、`season_id`、`match_id`、`round_id`、`payload`。客户端按 `event_id` 去重，按服务端时间排序。

### 7.3 API 原则

- 所有结算以服务端为准。
- 命令接口必须支持幂等键，防止断线重试重复提交。
- 前端不得接收超出当前可见范围的 AI 私密上下文，除非是导演上帝视角接口。
- 导演控制台和观众页面使用不同权限范围。
- 事件日志不可变；修正应追加新事件而不是覆盖旧事件。

---

## 8. 非功能需求

### 8.1 性能

- 首屏静态页面加载目标：桌面端 3 秒内可见核心框架。
- 实时事件从服务端产生到观战页显示目标：P95 小于 1 秒。
- 聊天流采用虚拟列表或分页，避免长场次导致 DOM 无限增长。
- 图表只渲染当前可见数据，历史数据按需加载。

### 8.2 稳定性

- 单个 AI 接口失败不能阻塞整轮。
- WebSocket 断开后自动重连并请求最新快照。
- 页面刷新后能恢复当前场次只读状态。
- 服务端保存每个状态迁移和结算依据。

### 8.3 安全与隐私

- 模型 API Key 只能存在服务端或密钥管理系统，不能进入浏览器包和事件推送。
- 私聊内容按观看权限过滤。
- 导演控制台需要认证；危险动作需要二次确认。
- 导出文件默认只在本地或受控存储中提供，不自动公开。

### 8.4 兼容性

- 支持 Chromium 系浏览器最新两个大版本。
- 优先支持 16:9 桌面屏幕和 1920×1080 OBS 画布。
- 支持 768px 以上平板和常见移动端浏览器的只读观战。

---

## 9. 验收场景

### 9.1 正常交易

1. 启动第 1 轮，页面显示角色分配和讨论倒计时。
2. 托付人完成选择，页面显示配对和出借金额。
3. 受托人完成还款，中央舞台展示三倍到账、实际还款和双方收益。
4. 交易记录、资金和信誉榜同步更新。

### 9.2 背叛事件

1. AI 公共频道承诺归还 20 金币。
2. 实际只归还 0 金币。
3. 页面标记“跑路”，托付人显示负收益，受托人显示正收益。
4. 信誉榜下降，关系状态更新为“被欺骗”，关键事件时间线增加记录。

### 9.3 API 失败

1. AI 接口超时。
2. 服务端最多重试 2 次。
3. 仍失败时生成降级动作，页面显示“系统降级”，其他 AI 继续推进。
4. 日志包含请求耗时、重试次数和默认动作。

### 9.4 断线恢复

1. 观战页在交易结算前断开 WebSocket。
2. 页面显示连接中状态，不允许重复提交任何动作。
3. 重连后获取服务端快照和缺失事件。
4. 交易只显示一次，资金和信誉与服务端一致。

### 9.5 公投平票

1. 第 2 或第 4 轮结算后进入公投。
2. 出现平票，页面显示平票状态和 30 秒重投倒计时。
3. 重投完成后显示最终淘汰者和遗言。
4. 被淘汰 AI 不再参与交易，但历史卡片和数据仍可查看。

---

## 10. 开发优先级与里程碑

### M1：可运行游戏闭环

- 单季第一场、6 个同模型 AI
- 游戏状态机、资金结算、交易记录
- 基础观战页和事件推送
- 异常降级与断线恢复

### M2：节目化观战体验

- 电影化信任驾驶舱样式落地
- 公共/私聊面板、信誉榜、事件舞台
- 公投、淘汰动画、遗言和 OBS 布局
- 导演控制台基本操作

### M3：系列化数据能力

- 3 季 15 场配置
- 跨场信誉、赛季排名和道德曲线
- 回放、关键事件标记和 CSV/Markdown 导出
- 多模型 API 适配层

### M4：扩展互动

- 模拟弹幕和观众投票
- 人类玩家特别篇
- 更多博弈框架和可配置压力维度

---

## 11. 待确认问题

以下问题不阻塞 M1 的同模型模拟，但在接入真实节目流程前需要确定：

1. “随机配对”是由系统直接随机生成，还是由托付人选择受托人后再处理冲突？当前按策划文档采用“托付人选择 + 冲突由受托人取舍”。
2. 第 6 轮受托人的还款是否允许超过本轮到账金额？当前按“0–全部到账金额”处理。
3. 跨场信誉榜是否对所有 AI 和观众完全公开，还是按第二季信息压力规则动态裁剪？当前由场次信息状态控制可见范围。
4. 模拟弹幕、家人角色和“真假数据”是否由独立系统生成，还是作为 AI 上下文的一部分注入？建议独立生成并记录为事件。
5. 真实厂商模型的名称、版本、品牌展示和 API 合规范围需要在接入前确认。

---

## 12. 变更记录

| 版本 | 日期 | 内容 |
|---|---|---|
| v1.0 | 2026-08-19 | 基于游戏策划文档建立功能、前端视觉、数据接口和验收规格 |

